import { ImageProperties } from "types"

export type Sitemap = ImageProperties

export interface DevelopmentPropertyPreview {
  bedrooms: number
  name: string
  slug: string
  location: Location
  omv: number
  initialShare: number
  propertyType: string
  listingStatus: string
}
