import { HeartIcon } from "@heroicons/react/outline"
import React from "react"

// we will pull the property details in to here and add to local storage

export default function AddToFavourites() {
  return <HeartIcon className="h-7 w-7 text-white hover:text-brand-green" />
}
